pub state<i16> count := state(0)

pub fn Counter() {
    mut i8 val := 2

    if count == 0 {
        val = 2
    } else {
        val = 4
    }

    f32 derived := (sum + count) / 2

    mut i32 count := 2
    fn increment(mut i32 prev, mut i64 out) {
        // Do something
    }


    return (
        <div>
            <p>The sum of 2 + 3 + 4 is {sum}</p>
            <p>Count: {count}</p>
            <button onclick={fn() { count++ }}>Increment</button>
        </div>
    )
}